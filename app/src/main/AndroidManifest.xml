<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.christian">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Christian">
        <activity android:name=".UpsCheck"
            android:label="Monthly UPS test"
            ></activity>
        <activity android:name=".Speedtest" />
        <activity
            android:name=".SettingsActivity"
            android:label="Settings" />
        <activity
            android:name=".WorkstationInfo"
            android:theme="@style/Theme.Christian.NoActionBar" />
        <activity android:name=".Ups" />
        <activity
            android:name=".Workstation"
            android:label="Workstation"
            android:theme="@style/Theme.Christian.NoActionBar" />
        <activity android:name=".testpage" />
        <activity
            android:name=".Capture"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity
            android:name=".ScrollingActivity"
            android:label="test"
            android:theme="@style/Theme.Christian.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>